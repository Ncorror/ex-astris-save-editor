# Gradle / Android
.gradle/
build/
app/build/
local.properties
*.apk
*.aab
*.ap_
*.dex
captures/

# IDE
.idea/
*.iml
.DS_Store

# save files, never commit personal data
*.save
*.save.jpg
*.bak

# Game files (never commit; the skin patch works on the player's own files)
*.ab

# Release signing keys (never commit)
*.jks
*.keystore
release-key.properties
